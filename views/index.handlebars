<h1>Eat Da Burger</h1>

<div class="container">
    <div class="row">
        <div class="col-md-6">
            <ul>
                {{#each answer}}
                <li>
                    <p>
                        {{this.name}}
                    </p>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>
</div>